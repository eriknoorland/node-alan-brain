/**
 * cans
 * @return {Object}
 */
const cans = (log, options) => {

  /**
   * Constructor
   */
  function constructor() {
    log('constructor', 'cans');

    // set pixy in right mode (s1)
  }

  /**
   * Start
   */
  function start() {

  }

  /**
   * Loop
   */
  function loop() {

  }

  constructor();

  return {
    start,
    loop,
  };
};

module.exports = cans;
